{
  "name": "feiniu-photo-heatmap",
  "version": "1.0.0",
  "description": "飞牛照片热力图 — 每日照片数量 GitHub 风格热力图",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "typecheck": "vue-tsc --noEmit",
    "preview": "vite preview",
    "server": "node server/index.js",
    "docker:build": "docker build -t feiniu-photo-heatmap:latest -f docker/Dockerfile .",
    "docker:run": "docker run -d -p 3000:3000 --name feiniu-photo-heatmap feiniu-photo-heatmap"
  },
  "dependencies": {
    "date-fns": "^3.3.1",
    "vue": "^3.4.21"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.0.4",
    "typescript": "^5.4.2",
    "vite": "^5.1.6",
    "vue-tsc": "^2.0.6"
  }
}
